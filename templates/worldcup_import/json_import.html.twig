{% extends 'base.html.twig' %}

{% block title %}Worldcup import data{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Worldcup import data! ✅</h1>

    {{ include('worldcup_import/_form.html.twig') }}

    {% if match_exists is defined and match_exists is same as 'exists' %}
        <div class="col-sm-12">
            <p>
                <a href="{{ path('app_export_data_matches') }}" target='_blank'>Worldcup Matches</a>
            </p>
        </div>
    {% endif %}

    {% if match_exists is defined and match_exists is same as 'exists' and team_exists is defined and team_exists is same as 'exists' %}
        <div class="col-sm-12">
            <p>
                <a href="{{ path('app_export_data_teams_results') }}" target='_blank'>Worldcup Team Results</a>
            </p>
        </div>
    {% endif %}

</div>
{% endblock %}
